# PdfGenerate

🛠️ Instalação e Configuração
Este projeto utiliza a biblioteca QuestPDF para a renderização dos documentos. Siga os passos abaixo para configurar o ambiente:

1. Instalar o Pacote NuGet
Abra o terminal na pasta raiz do projeto e execute o comando abaixo:

Bash
dotnet add package QuestPDF
